# 会话工作台

`MessageBody` 将助手正文中的 `[标题](memory:节点ID)` 显示为可点击的行内引用，复用 `openEntry` 的受控条目/来源查看。只允许当前消息服务端引用列表中的 ID；无效引用显示“来源不可用”，不执行模型给出的任意地址。用户原文保持文字展示，复制助手回复不会包含内部引用地址。

明确问候由 Memory 分类器本地回复，显示“本地回复”和“对话来源”，不误标为 wiki 已整理。
