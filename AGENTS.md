# Agent 开发约定

- frontend 使用 Dioxus 和独立共享组件库，engine 为无界面、无宿主依赖的 Rust 执行库，backend 负责 AIO 鉴权、加密、持久化和受控出站。各层同仓发布。
- 业务不写 CSS，布局和主题能力放入 dioxus-admin-workbench。新建、设置和确认操作使用共享 Dialog。
- 子插件名统一 aio-plugin-agent-<功能名>，Memory 经宿主桥授权调用，不直接访问子插件数据库。
- 模型及工具密钥不能回传浏览器；出站必须经过清单与宿主共同授权，不跟随重定向，不读取全局代理或第三方密钥环境变量。
- 按功能组织，目录有 README，中文注释，单文件不超过 800 行。模型与实现分离。
- 验证真实 wasm32 前端、服务回归和桌面/移动端浏览器后，只提交并推送本次完成修改。
